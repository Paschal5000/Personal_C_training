#include<stdio.h>

// A program that checks whether 
// a number is divisible by 97 or not

int main()
{
int num;
printf("Enter the number:");
scanf("%d", &num);

if (num % 97 == 0)
printf("is divisible by 97");
else
printf("is not divisible by 97");

return(0);
}
